<Page
    x:Class="StoreManagementMobile.Presentation.OrderSuccessPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#F5F5F5">

    <Grid>
        <StackPanel 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            MaxWidth="400"
            Spacing="24"
            Padding="30">
            
            <!-- Icon dấu tích xanh -->
            <Ellipse 
                Width="120" 
                Height="120" 
                Fill="#4CAF50">
            </Ellipse>
            <TextBlock 
                Text="✓"
                FontSize="80"
                FontWeight="Bold"
                Foreground="White"
                HorizontalAlignment="Center"
                Margin="0,-120,0,0"/>
            
            <!-- Title -->
            <TextBlock 
                Text="Đặt hàng thành công!"
                FontSize="28"
                FontWeight="Bold"
                HorizontalAlignment="Center"
                Foreground="#4CAF50"
                Margin="0,20,0,0"/>
            
            <!-- Message -->
            <TextBlock 
                Text="Cảm ơn bạn đã mua hàng! 🙏"
                FontSize="16"
                HorizontalAlignment="Center"
                Foreground="#666"
                TextWrapping="Wrap"/>
            
            <!-- Thông tin đơn hàng -->
            <Border 
                Background="White" 
                CornerRadius="12" 
                Padding="20"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Margin="0,10,0,0">
                <StackPanel Spacing="12">
                    <TextBlock>
                        <Run Text="💰 Tổng tiền: " FontWeight="SemiBold"/>
                        <Run x:Name="TotalText" FontWeight="Bold" FontSize="18" Foreground="#4CAF50"/>
                    </TextBlock>
                    
                    <TextBlock>
                        <Run Text="📍 Giao tới: " FontWeight="SemiBold"/>
                        <Run x:Name="CustomerNameText"/>
                    </TextBlock>
                    
                    <TextBlock>
                        <Run Text="📞 Liên hệ: " FontWeight="SemiBold"/>
                        <Run x:Name="CustomerPhoneText"/>
                    </TextBlock>
                </StackPanel>
            </Border>
            
            <!-- Nút quay về trang chủ -->
            <Button 
                Content="🏠 Quay về trang chủ"
                Click="BackToHome_Click"
                HorizontalAlignment="Stretch"
                Background="#4CAF50"
                Foreground="White"
                FontSize="16"
                FontWeight="SemiBold"
                Height="50"
                CornerRadius="8"
                BorderThickness="0"
                Margin="0,10,0,0">
            </Button>
            
        </StackPanel>
    </Grid>
</Page>
